import React from 'react';

const FeedbackFormPage = () => {
    return (
        <h2>This is the page where users will go to when they complete their session</h2>
    );
}

export default FeedbackFormPage;