// const pool = require('../../db');

// const insertComment = async (bookingId, comment, date, time) => {
//   pool.query(
//     'INSERT INTO comments (booking_id, comment, date_col, time_stamp) VALUES ($1, $2)',
//     [bookingId, comment]
//   );
// }

// module.exports = { insertComment }