const pool = require('../../db');

const dashboard = (req, res) => {
  res.send('private info!');
};

const getUpcomingInterviews = async (userId) => {

};

const getPastInterviews = async (userId) => {

};

module.exports = {
  dashboard
}